# use the command "crontab -l" to see the list of scheduled script
# use the command "crontab -e" to schedule the script

# It will schedule the script to run on the specified time
# 34 20 * * * /path/to/script

# Redirecting the output to output.txt file
# 34 20 * * * /path/to/script > output.txt
